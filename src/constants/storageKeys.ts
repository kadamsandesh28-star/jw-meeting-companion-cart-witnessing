/**
 * Local Storage keys used throughout the application.
 * Keeping them in one place prevents typos and makes
 * future changes much easier.
 */
export const STORAGE_KEYS = {
  bibleReadingProgress: "bible-reading-progress",
} as const;