# Cart Witnessing v5

- Congregation name is now read from the main Congregation Settings profile.
- Default congregation name: Vadodara East Congregation.
- Existing placeholder names are migrated safely.
- PDF and Word exports use the congregation name as the report heading.
- Export filenames include the congregation name.
- Cart Witnessing header shows the congregation name with a quick Edit button linking to Settings.
- Daily/Weekly, Saturday priority, up to two day captains, timed pairs, PDF/Word export and Share PDF are retained.

## PDF polish update
- PDF schedule rows now sort by actual start time (ascending), not text order.
- PDF uses a clean A4 landscape navy/green layout with a week-start card, alternating table rows, captain summary panel, and footer.
- PDF uses an original simple vector cart illustration rather than a copied photograph/logo.
- Word export now uses the same ascending time sort.
